import { IchangeDirective } from './ichange.directive';

describe('IchangeDirective', () => {
  it('should create an instance', () => {
    const directive = new IchangeDirective();
    expect(directive).toBeTruthy();
  });
});
